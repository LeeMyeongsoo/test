<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.site.p0830.mapper.MemberMapper">
   
    <!-- select 1개 검색 - login  -->
	<select id="selectMemberOne" resultType="com.site.p0830.vo.MemberVo">
		select * from member where id=#{id} and pw=#{pw}
	</select>
	

</mapper>